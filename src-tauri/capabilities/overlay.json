{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "overlay",
  "description": "Capabilities for the overlay capture window",
  "windows": ["overlay"],
  "permissions": [
    "core:default"
  ]
}
